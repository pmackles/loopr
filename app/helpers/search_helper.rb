module SearchHelper
#  def place_description(place, near_prefix = "near", in_prefix = "in")
#    if @place.city && @place.state      
#      "#{near_prefix} #{@place.city.titleize}, #{@place.state_full.titleize}"
#    else
#      "#{in_prefix} #{@place.state_full.titleize}"
#    end
#  end
end
